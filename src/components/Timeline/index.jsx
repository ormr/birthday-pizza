import React from "react"
import { StaticImage } from "gatsby-plugin-image"

const Timeline = ({ elementsCount = 3, users }) => {
  return (
    <div>{users.map((item) => )}</div>
  );
}

const BirthdayMan = () => (
  <div>
    <Avatar src= />
    <h3></h3>
  </div>
);

const Avatar = ({ src }) => (
  <div>
    <img src={} />
  </div>
);

export default Timeline;
